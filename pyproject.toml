[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "python-examples"
version = "0.3.0"
description = "Algorithms and systems examples repo"
readme = "README.md"
requires-python = ">=3.9"

dependencies = [
    "garak>=0.9.0.11.post1",
    "numpy>=1.23.5",
    "torch>=2.8.0",
    "transformers>=4.57.6",
]

[tool.setuptools]
package-dir = {"" = "."}

[tool.setuptools.packages.find]
where = ["."]
include = ["Algorithms*", "LLMs*", "mixins*"]

[dependency-groups]
dev = [
    "pre-commit>=3.5.0",
    "pydantic>=2.0,<3",
    "pytest-cov>=4.0,<5",
    "torch>=2.2,<3",
]
